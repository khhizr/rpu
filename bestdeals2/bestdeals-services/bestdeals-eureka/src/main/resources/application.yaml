spring:
  application:
    name: bestdeals-eureka-server

server:
  port: 8000
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8000/eureka